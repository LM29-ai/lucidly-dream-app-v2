// android/settings.gradle  (Groovy, legacy include style)

pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
  }
}

rootProject.name = 'Lucidly'
include ':app'

// Include RN and Expo settings via legacy apply-from (no plugins{} block)
apply from: file("../node_modules/@react-native/gradle-plugin/settings.gradle")
apply from: file("../node_modules/expo-modules-autolinking/android/include.gradle")
